#!/usr/bin/env bash
set -euo pipefail

qmd_entry="$(npm root -g)/@tobilu/qmd/dist/cli/qmd.js"
[[ -f "$qmd_entry" ]] || {
  echo "qmd-node: @tobilu/qmd is not installed globally" >&2
  exit 1
}

exec node "$qmd_entry" "$@"
